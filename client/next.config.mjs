/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'fastly.picsum.photos', 'tablica.com', 'images.pexels.com']
    }
};

export default nextConfig;
